# add your local .bashrc customization here

#if [ -t 0 ]; then
#	# if interactive shell
#	unalias ls
#fi

source /etc/environment 
export TERM="xterm-256color"
#PATH=/home/project/utils/bin:/sbin/:$PATH

alias jk='cd /local/tesheng/'
alias grep='grep --color=yes '
alias vi='vim'
alias tig='/usr/bin/tig'
alias ctags='/usr/bin/ctags'
alias gd='git diff'
alias gl='git log'
alias gst='git status'
alias gone='git log --oneline'
alias ..='cd ../../'
alias ...='cd ../../../'
alias jb='jobs -l'
alias ba='vi /home/users/tesheng/.bashrc.private'
alias so='source /home/users/tesheng/.bashrc.private'
alias se='source /etc/environment'
alias kq="ps auxf | grep tesheng | grep qemu | awk '{print \$2}' | xargs kill -9"

source ~/.git-prompt.sh
#function parse_git_branch { 
#   git branch --no-color 2> /dev/null | sed -e '/^[^*]/d' -e 's/* \(.*\)/(\1)/' 
#} 
export PS1="\u[\H] \033[1;34m\A \033[1;33m\w \033[6;32m$(__git_ps1 "(%s)")\033[m\n\$ "

#export PS1="\u[\H] \033[1;34m\A \033[1;33m\w \033[6;32m$(__git_ps1 "(%s)")\033[m\n\$ "
#export PS1="\u[\H] \033[1;34m\A \033[1;33m\w \033[6;32m$(parse_git_branch)\033[m\n\$ "

##QEMU##
function qemu64(){
	export PATH=/local/tesheng/qemu-riscv/qemu-riscv-package/riscv-gnu-toolchain/riscv64/bin:$PATH
	export ARCH=riscv CROSS_COMPILE=riscv64-unknown-linux-gnu-
	export CFLAGS="" LDFLAGS=""
}

function qemu32(){
	#export PATH=/local/tesheng/qemu-riscv/qemu-riscv-package/riscv-gnu-toolchain/riscv32/bin:$PATH
	export PATH=/local/tesheng/qemu-riscv/buildroot_rv32/output/host/bin:$PATH
	export ARCH=riscv CROSS_COMPILE=riscv32-buildroot-linux-gnu-
	export CFLAGS="" LDFLAGS=""
}

function no(){
	source /etc/environment	
	export ARCH="" CROSS_COMPILE=""
	export CFLAGS="" LDFLAGS=""
}

function rv64(){
#	export PATH=/NOBACKUP/sqa3/NFSTest/build-bsp510/build-toolchain/linux/nds64le-linux-glibc-v5d/bin/:$PATH
#	export PATH=/home/users3/sqa/BSP-v510/linux/toolchains-bin/nds64le-linux-glibc-v5d/bin/:$PATH
	export PATH=/NOBACKUP/sqa3/NFSTest/build-ast322/build-toolchain/linux/nds64le-linux-glibc-v5d/bin/:$PATH
	#export PATH=/NOBACKUP/sqa3/NFSTest/build-ast322/build-toolchain/linux/nds64le-elf-mculib-v5d/bin:/$PATH
#	export PATH=/local/tesheng/linux_devel_pack/eddie_kuo_toolchain/nds64le-linux-glibc-v5d/bin:/$PATH
	export ARCH=riscv CROSS_COMPILE=riscv64-linux-
	#export CFLAGS="-march=rv64v5 -mabi=lp64" LDFLAGS="-march=rv64v5 -mabi=lp64"
	export CFLAGS="-march=rv64v5" LDFLAGS="-march=rv64v5"
#	export CFLAGS="-march=rv64v5d" LDFLAGS="-march=rv64v5d -static"
	#export CFLAGS="-march=rv64v5" LDFLAGS="-march=rv64v5"
	#export ARCH=riscv CROSS_COMPILE=riscv64-unknown-linux-gnu-
}

function rv32(){
#	export PATH=/local/tesheng/build-system-3/toolchain/nds32le-linux-glibc-v5d/bin:$PATH
#	export PATH=/home/users/sqa/BSP-v510/linux/toolchains-bin/nds32le-linux-glibc-v5d/bin/:$PATH
#	export PATH=/NOBACKUP/sqa3/NFSTest/build-ast322/build-toolchain/linux/nds32le-linux-glibc-v5d/bin/:$PATH
	export PATH=/local/tesheng/build-system-3/toolchain/nds32le-linux-glibc-v5d/bin/:$PATH
#	export PATH=/local/tesheng/build-system/build-system-3/toolchain/nds32le-linux-glibc-v5d/bin/:$PATH
#	export PATH=/local/tesheng/benchmark/riscv32-linux-7.4-2.29-4.17/bin/:$PATH
	export ARCH=riscv CROSS_COMPILE=riscv32-linux-
	export CFLAGS="-march=rv32v5d" LDFLAGS="-march=rv32v5d" #ARCH_FLAGS="-march=rv32v5d"
}

function v3(){
#export PATH=/local/tesheng/V3/nds32le-linux-glibc-v3f/bin/:$PATH
	export PATH=/local/tesheng/V3/nds32le-linux-glibc-v3-upstream/bin/:$PATH
	export ARCH=nds32 CROSS_COMPILE=nds32le-linux-
	export CFLAGS="" LDFLAGS=""
}

if [ $# -gt 0 ]
then
    $1
    echo $1
fi

export http_proxy=http://cache1:3128/
export https_proxy=http://cache1:3128/
